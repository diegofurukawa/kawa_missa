# Stack Tecnológica - Kawa Missa

**Projeto:** Kawa Missa - Gestão Paroquial
**Versão:** 0.1.0
**Última Atualização:** 2026-01-12
**Status:** Produção

---

## 📋 Sumário Executivo

Kawa Missa é uma aplicação web Progressive Web App (PWA) para gestão e consulta de horários de missas paroquiais. Desenvolvida com Next.js 16, TypeScript, Prisma v7 e PostgreSQL, segue arquitetura monolítica orientada a Server Components com multi-tenancy.

---

## 🎯 Stack Principal

### Framework & Runtime

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **Next.js** | 16.1.1 | Framework Web | App Router, Server Components, SSR/SSG, API Routes |
| **React** | 19.2.3 | Biblioteca UI | Base do Next.js, Server Components nativos |
| **Node.js** | 20.x (Alpine) | Runtime | LTS, compatível com Prisma v7, container otimizado |
| **TypeScript** | 5.x | Linguagem | Type-safety, strict mode, melhor DX |

### Base de Dados & ORM

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **PostgreSQL** | 14+ | Database | Relacional, JSON support, multi-tenancy |
| **Prisma** | 7.2.0 | ORM | Type-safe queries, migrations, driver adapters |
| **@prisma/adapter-pg** | 7.2.0 | Adapter | Connection pooling via pg.Pool |
| **pg** | 8.16.3 | Driver | PostgreSQL driver nativo |

**Configuração Prisma:**
- Preview Features: `driverAdapters`
- Connection: Pool-based via `@prisma/adapter-pg`
- Migrations: Automáticas via `prisma migrate deploy` no entrypoint Docker

### Autenticação & Segurança

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **NextAuth.js** | 5.0.0-beta.30 | Auth | Integração nativa Next.js 16, JWT, Credentials Provider |
| **bcryptjs** | 3.0.3 | Hashing | Password hashing (bcrypt também instalado) |
| **JWT** | via NextAuth | Token | Session management via JWT callbacks |

**Estratégia de Autenticação:**
- Provider: Credentials (email/password)
- Session: JWT com dados estendidos (id, role, tenantId)
- Callbacks: Token e Session customizados
- Middleware: Proteção de rotas `/dashboard/*`

### Estilização & UI

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **Tailwind CSS** | 4.x | Framework CSS | Utility-first, mobile-first, customizável |
| **@tailwindcss/postcss** | 4.x | PostCSS | Processador Tailwind v4 |
| **clsx** | 2.1.1 | Utilitário | Conditional classes |
| **tailwind-merge** | 3.4.0 | Utilitário | Merge conflicting Tailwind classes |
| **lucide-react** | 0.562.0 | Ícones | Ícones SVG React-friendly |

**Cores Customizadas:**
- Primary: `#6d7749` (verde oliva)
- Secondary: `#6546b8` (roxo)
- Background: `#f6f5f8` (cinza claro)

### Feedback & UX

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **sonner** | 2.0.7 | Toast | Notificações não-bloqueantes |

### Validação & Schemas

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **zod** | 4.2.1 | Validação | Type-safe schemas, integração Prisma/Forms |

### Utilitários & Data

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **date-fns** | 4.1.0 | Data/Hora | Formatação, parsing, manipulação de datas |
| **cron-parser** | 4.9.0 | Cron | Parse/validação de expressões cron |

### PWA & Service Workers

| Tecnologia | Versão | Papel | Justificativa |
|------------|--------|-------|---------------|
| **@ducanh2912/next-pwa** | 10.2.9 | PWA | Fork moderno, suporte Next.js 16 + App Router |
| **web-push** | 3.6.7 | Push | Notificações push via VAPID |

**Estratégias de Cache (Workbox):**
- **Páginas Públicas**: `NetworkFirst` (2h TTL, fallback offline)
- **Imagens**: `CacheFirst` (30 dias TTL)
- **APIs**: `NetworkFirst` (5min TTL)

**Manifest:**
- Instalável em Android, iOS, Desktop
- Ícones: 72px até 512px (maskable)
- Tema: `#6c7948`
- Idioma: `pt-BR`

### Push Notifications

**Backend:**
- `web-push` para envio via VAPID
- Modelo `PushSubscription` no Prisma
- API Routes: `/api/push/{subscribe,unsubscribe,send}`

**Frontend:**
- Componente `PushNotificationManager`
- Service Worker handlers: `push`, `notificationclick`
- Permissões via browser APIs

---

## 🏗️ Arquitetura

### Estrutura de Pastas

```
kawa_missa/
├── .Agent/                          # Governança de IA
│   ├── .AgentRules.md               # Regras e papéis
│   ├── .AgentGoals.md               # Objetivos do projeto
│   ├── .CodeConventions.md          # Convenções de código
│   ├── .Stack.md                    # Este arquivo
│   └── .plans/                      # Planos de implementação
│       ├── 0001_bootstrap-projeto.md
│       ├── 0002_database_schema.md
│       ├── 0003_auth_onboarding.md
│       ├── 0004_mass_management.md
│       ├── 0005_dashboard_carousel.md
│       ├── 0006_deployment.md
│       ├── 0007_metados.md
│       └── reports/                 # Relatórios de execução
│
├── app/                             # App Router (Next.js)
│   ├── layout.tsx                   # Root layout (metadata, providers)
│   ├── page.tsx                     # Landing page
│   ├── globals.css                  # Tailwind base + custom CSS
│   ├── providers.tsx                # SessionProvider (NextAuth)
│   │
│   ├── login/                       # Página de login
│   ├── onboarding/                  # Cadastro de novos tenants
│   ├── select-tenant/               # Seleção de tenant (multi-tenancy)
│   ├── offline/                     # Fallback PWA offline
│   │
│   ├── dashboard/                   # Área autenticada
│   │   ├── layout.tsx               # Layout com sidenav
│   │   ├── page.tsx                 # Dashboard principal
│   │   ├── masses/                  # CRUD de missas
│   │   ├── config/                  # Configurações (cron, roles)
│   │   ├── organization/            # Dados da paróquia
│   │   └── public/                  # Página pública (carrossel)
│   │
│   ├── ui/                          # Componentes UI
│   │   ├── button.tsx               # Primitivos
│   │   ├── tag.tsx, tag-input.tsx
│   │   ├── date-input.tsx, time-input.tsx
│   │   ├── pagination.tsx
│   │   ├── share-button.tsx
│   │   ├── rich-text-editor.tsx
│   │   ├── push-notification-manager.tsx
│   │   │
│   │   ├── dashboard/               # Componentes do dashboard
│   │   │   ├── sidenav.tsx
│   │   │   ├── header.tsx
│   │   │   ├── footer.tsx
│   │   │   ├── public-header.tsx
│   │   │   ├── login-modal.tsx
│   │   │   ├── mass-carousel.tsx
│   │   │   └── catholic-message-banner.tsx
│   │   │
│   │   ├── masses/                  # Componentes de missas
│   │   │   ├── create-form.tsx
│   │   │   ├── edit-form.tsx
│   │   │   ├── edit-participants-modal.tsx
│   │   │   └── delete-button.tsx
│   │   │
│   │   ├── config/                  # Componentes de configuração
│   │   │   ├── form.tsx
│   │   │   ├── cron-builder.tsx
│   │   │   └── delete-button.tsx
│   │   │
│   │   ├── organization/            # Formulários de organização
│   │   │   └── form.tsx
│   │   │
│   │   └── login/                   # Componentes de login
│   │       └── login-form.tsx
│   │
│   └── api/                         # API Routes
│       ├── auth/                    # NextAuth routes
│       └── push/                    # Push notifications
│           ├── subscribe/
│           ├── unsubscribe/
│           └── send/
│
├── lib/                             # Lógica de negócio
│   ├── actions.ts                   # Server Actions (com 'use server')
│   ├── data.ts                      # Data fetching (queries Prisma)
│   ├── definitions.ts               # Tipos e Zod schemas
│   ├── prisma.ts                    # Singleton Prisma Client
│   └── push-notifications.ts        # Utilities push
│
├── prisma/                          # Schema e migrations
│   ├── schema.prisma                # Modelos de dados
│   └── migrations/                  # Migrations versionadas
│
├── types/                           # Tipos globais
│   └── next-auth.d.ts               # Extend NextAuth types
│
├── auth.ts                          # NextAuth config principal
├── auth.config.ts                   # NextAuth auth config
├── middleware.ts                    # Middleware de proteção de rotas
├── next.config.ts                   # Config Next.js + PWA
├── tsconfig.json                    # Config TypeScript
├── postcss.config.mjs               # Config PostCSS
├── Dockerfile                       # Multi-stage build
├── docker-compose.yml               # Orquestração de containers
├── package.json                     # Dependências
└── .env                             # Variáveis de ambiente (não versionado)
```

---

## 🗄️ Schema de Dados (Prisma)

### Modelos

#### **Tenant**
- Multi-tenancy: root de isolamento de dados
- Campos: `id`, `name`, `denomination`, `legalName`, `document`, `phone`, `email`, `responsibleName`, `createdAt`
- Relações: `users[]`, `masses[]`, `configs[]`, `address?`

#### **User**
- Usuários do sistema vinculados a um tenant
- Campos: `id`, `tenantId`, `name`, `email`, `phone`, `password`, `role`, `createdAt`
- Relações: `tenant`, `pushSubscriptions[]`

#### **Address**
- Endereço da organização (1:1 com Tenant)
- Campos: `zipCode`, `street`, `number`, `complement`, `neighborhood`, `city`, `state`, `country`

#### **Config**
- Configurações de cron e papéis de participantes
- Campos: `cronConfig` (JSON), `participantConfig` (JSON)
- Relações: `tenant`, `masses[]`

#### **Mass**
- Missas agendadas
- Campos: `id`, `slug`, `tenantId`, `configId`, `date`, `type`, `description`, `participants` (JSON)
- Unique: `[tenantId, slug]`
- Relações: `tenant`, `config?`

#### **PushSubscription**
- Subscriptions de notificações push
- Campos: `userId`, `endpoint`, `p256dh`, `auth`
- Relações: `user`

---

## 🚀 Deployment & DevOps

### Docker

**Dockerfile (Multi-stage):**
1. **Stage 1: deps** - Instalação de dependências
2. **Stage 2: builder** - Build do Next.js + Prisma generate
3. **Stage 3: runner** - Imagem de produção (Alpine Node 20)

**Output Mode:** `standalone` (otimizado para containers)

**Entrypoint Script:** [`scripts/start.sh`](scripts/start.sh)
```bash
#!/bin/sh
echo "Running migrations..."
npx prisma migrate deploy
echo "Starting application..."
node server.js
```

### Docker Compose

- **Serviço:** `kawa_missa`
- **Network:** `furukawatech-network` (externa)
- **IP Fixo:** `172.30.0.55`
- **Porta:** `3115` (interna/externa configurável)
- **Restart Policy:** `unless-stopped`

### Variáveis de Ambiente

**Críticas:**
- `DATABASE_URL` ou `DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`
- `DB_SSL` (true/false)
- `AUTH_SECRET`, `NEXTAUTH_SECRET`, `JWT_SECRET`
- `NEXT_PUBLIC_VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_EMAIL`

**Opcionais:**
- `NEXTAUTH_URL` (produção)
- `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_APP_URL`, `NEXT_PUBLIC_BASE_URL`

### Build & Deployment

**Comandos:**
```bash
# Development
npm run dev

# Build
npm run build

# Produção
npm run start

# Docker
docker build -t kawa_missa:latest .
docker-compose up -d

# Migrations
npx prisma migrate dev --name <name>    # Dev
npx prisma migrate deploy               # Produção
```

---

## 🔐 Segurança

### Práticas Implementadas

1. **Autenticação:**
   - Passwords hasheados com bcryptjs
   - JWT com expiração configurável
   - Session isolada por tenant

2. **Autorização:**
   - Middleware protege rotas privadas
   - Server Actions validam `tenantId` via session
   - Queries Prisma filtradas por tenant

3. **Validação:**
   - Zod schemas em todas as Server Actions
   - Sanitização de inputs
   - Retorno de erros genéricos ao cliente

4. **Database:**
   - SSL configurável via `DB_SSL`
   - Connection pooling com timeouts
   - Prepared statements (Prisma)

5. **Headers:**
   - `AUTH_TRUST_HOST: true` em produção
   - CORS implícito via Next.js

---

## 📊 Performance & Otimizações

### Build

- **Output:** `standalone` (reduz size da imagem)
- **Tree-shaking:** Automático via Next.js
- **Minificação:** CSS/JS via SWC
- **Image Optimization:** `sharp` (devDep)

### Runtime

- **Server Components:** Default, reduz bundle JS
- **Connection Pooling:** `pg.Pool` via Prisma adapter
- **Static Generation:** Páginas onde aplicável
- **ISR:** Revalidação via `revalidatePath()` em Server Actions

### PWA

- **Service Worker:** Gerado via `@ducanh2912/next-pwa`
- **Cache Strategy:**
  - Assets estáticos: `CacheFirst`
  - Dados dinâmicos: `NetworkFirst`
- **Offline Fallback:** [/offline](app/offline/page.tsx)

---

## 🧪 Testing & Quality

### Ferramentas Configuradas

- **ESLint:** `eslint-config-next` (rules do Next.js)
- **TypeScript:** Strict mode habilitado

### Áreas de Melhoria

- ❌ Testes unitários (não configurado)
- ❌ Testes E2E (não configurado)
- ⚠️ Alguns `any` types ainda presentes (technical debt)

---

## 🌐 Internacionalização

- **Idioma:** Português (pt-BR)
- **Locale:** Configurado em `app/layout.tsx` (`lang="pt-BR"`)
- **Mensagens:** Hard-coded em português
- **Datas:** Formatação via `date-fns` (locale pt-BR quando necessário)

---

## 📱 Progressive Web App (PWA)

### Capabilities

- ✅ Instalável (Android, iOS, Desktop)
- ✅ Offline-first (páginas visitadas + assets)
- ✅ Push Notifications (via VAPID)
- ✅ App Shell (Service Worker)
- ✅ Manifest (`/manifest.json`)

### Metadados & SEO

- **Open Graph:** Imagens 1200x630, locale pt-BR
- **Twitter Cards:** `summary_large_image`
- **Sitemap:** Dinâmico ([`app/sitemap.ts`](app/sitemap.ts)) com URLs de tenants
- **Robots.txt:** Permite páginas públicas, bloqueia dashboard privado
- **Metadata Dinâmico:** `generateMetadata()` em `/dashboard/public`

### Ícones

- **PWA:** 72, 96, 128, 144, 152, 192, 384, 512px (maskable)
- **iOS:** `apple-touch-icon.png` (180x180)
- **Favicons:** 16x16, 32x32
- **Windows:** `browserconfig.xml`

---

## 🔄 Convenções de Desenvolvimento

### Commits

- Padrão: Livre (sem conventional commits obrigatório)
- Branches: `main` como default

### Code Style

- **Components:** PascalCase (`UserCard.tsx`)
- **Utils:** camelCase (`formatDate.ts`)
- **Folders:** kebab-case (`user-profile`)
- **Imports:** Absolute `@/*` preferidos

### Server Actions

- Centralizados em [`lib/actions.ts`](lib/actions.ts)
- Diretiva `'use server'` no topo do arquivo
- Retorno padrão: `{ message?, errors?, success? }`
- Validação obrigatória com Zod
- `revalidatePath()` após mutações

### Forms

- `useActionState` para Server Actions
- `FormData` como input
- Toast via `sonner` em `useEffect`

---

## 📚 Documentação de Referência

### Principais Tecnologias

- **Next.js 16:** https://nextjs.org/docs
- **React 19:** https://react.dev/blog/2024/12/05/react-19
- **Prisma v7:** https://www.prisma.io/docs
- **NextAuth v5:** https://authjs.dev/getting-started/introduction
- **Tailwind CSS v4:** https://tailwindcss.com/docs/v4-beta
- **@ducanh2912/next-pwa:** https://ducanh-next-pwa.vercel.app/

### Planos de Implementação

Todos os planos executados estão em [`.Agent/.plans/`](.Agent/.plans/):
- [0001_bootstrap-projeto.md](.Agent/.plans/0001_bootstrap-projeto.md) ✅
- [0002_database_schema.md](.Agent/.plans/0002_database_schema.md) ✅
- [0003_auth_onboarding.md](.Agent/.plans/0003_auth_onboarding.md) ✅
- [0004_mass_management.md](.Agent/.plans/0004_mass_management.md) ✅
- [0005_dashboard_carousel.md](.Agent/.plans/0005_dashboard_carousel.md) ✅
- [0006_deployment.md](.Agent/.plans/0006_deployment.md) ✅
- [0007_metados.md](.Agent/.plans/0007_metados.md) ⚠️ (status indefinido)

---

## 🐛 Troubleshooting Comum

### Database Connection

**Sintoma:** `ECONNREFUSED` ou timeout
**Soluções:**
- Verificar `DB_HOST` usa service name Docker (ex: `host_database`)
- Confirmar `DB_SSL='false'` se banco não usa SSL
- Checar se container DB está na mesma network
- Inspecionar logs do pool: event listeners `error`/`connect` em [`lib/prisma.ts`](lib/prisma.ts)

### Migration Failures

**Sintoma:** Erro P3009 (failed migrations)
**Soluções:**
- Limpar migrations falhadas: `DELETE FROM _prisma_migrations WHERE finished_at IS NULL`
- Verificar ordem cronológica dos timestamps
- Considerar consolidação de migrations (reset dev DB)

### Build Failures

**Sintoma:** Prisma WASM não encontrado
**Soluções:**
- Verificar cópia do WASM no Dockerfile (linha 75-78)
- Confirmar `npx prisma generate` rodou no stage builder
- Checar se `prisma` CLI está no `node_modules` do runner

### PWA Não Instala

**Sintoma:** Prompt de instalação não aparece
**Soluções:**
- Verificar HTTPS (obrigatório exceto localhost)
- Confirmar `manifest.json` acessível
- Testar no Chrome DevTools > Application > Manifest
- Lighthouse audit para checklist PWA

---

## 📈 Métricas & Performance

### Bundle Size (Next.js)

- **Output:** `standalone` (~80MB imagem Docker runner)
- **JS Bundle:** Otimizado via Server Components
- **CSS:** Tailwind purgado automaticamente

### Lighthouse (Target)

- **Performance:** 90+
- **Accessibility:** 90+
- **Best Practices:** 90+
- **SEO:** 90+
- **PWA:** 90+

---

## 🔮 Roadmap Técnico

### Pendente (Plano 0007)

- ✅ PWA configuração base
- ✅ Service Worker + caching
- ✅ Manifest + ícones
- ✅ Push notifications backend/frontend
- ✅ Metadados dinâmicos
- ✅ Sitemap + robots.txt
- ⚠️ **Status indefinido - requer auditoria**

### Melhorias Futuras (Não Planejadas)

- Testes automatizados (Jest + Playwright)
- CI/CD pipeline (GitHub Actions)
- Monitoramento (Sentry, Datadog)
- i18n (multi-idioma)
- Microservices (se escala exigir)

---

## 📞 Contato & Manutenção

**Repositório:** `kawa_missa`
**Ambiente:** Produção via Docker + Portainer
**Governança:** Sistema de IA com roles Auditor/Executor
**Documentação Viva:** Este arquivo é mantido pelo AGENT_AUDITOR

---

**Última Auditoria:** 2026-01-12
**Próxima Revisão:** Após execução do Plano 0007 (metadados/PWA)
