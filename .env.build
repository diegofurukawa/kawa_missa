# ===================================================
# .env.build - Variáveis para BUILD do Docker
# ===================================================
# Este arquivo contém valores dummy/placeholder apenas para
# permitir que o build do Next.js complete sem erros.
# Os valores reais são injetados via docker-compose/Portainer
# no runtime do container.
# ===================================================

# === Database (dummy para build) ===
DATABASE_URL="postgresql://dummy:dummy@localhost:5432/dummy"

# === Authentication (dummy para build) ===
AUTH_SECRET="build-time-dummy-secret-not-for-production-min-32-chars"
NEXTAUTH_SECRET="build-time-dummy-secret-not-for-production-min-32-chars"
NEXTAUTH_URL="http://localhost:3115"
JWT_SECRET="build-time-dummy-secret-not-for-production"

# === Public URLs (dummy para build) ===
NEXT_PUBLIC_API_URL="http://localhost:3115"
NEXT_PUBLIC_APP_URL="http://localhost:3115"
NEXT_PUBLIC_BASE_URL="http://localhost:3115"
NEXT_PUBLIC_DEFAULT_HOSTS="http://localhost:3115"
NEXT_PUBLIC_CEP_API_URL="https://postalcode.furukawatech.com.br/api/PostalCode"
